name: incident_backend
description: Turns a captured production incident into a Jira ticket and a Slack alert.
publish_to: none
version: 1.0.0

environment:
  sdk: ^3.5.0

dependencies:
  shelf: ^1.4.2
  http: ^1.2.2
  native_stack_traces: ^0.7.0

dev_dependencies:
  lints: ^5.0.0
  test: ^1.25.0
